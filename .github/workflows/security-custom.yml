name: Security-Full-Summary    # custom + positive, console only

on:
  push:
    branches: [ main, develop ]
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Semgrep
        run: pip install semgrep

      # ---------- 1. Custom 70-rule set ----------
      - name: Run custom Spring rules
        run: |
          echo "🔍 Scanning custom rule-set (70 rules)..."
          semgrep --config=.semgrep/spring-custom.yml \
                  --error \
                  --output=custom.txt \
                  src/ || true

      # ---------- 2. Positive 7-rule set ----------
      - name: Run beginner positive rules
        run: |
          echo "🔍 Scanning positive rule-set (7 rules)..."
          semgrep --config=.semgrep/spring-positive.yml \
                  --error \
                  --output=positive.txt \
                  src/ || true

      # ---------- 3. Console summary ----------
      - name: Generate summary
        if: always()
        run: |
          echo "=============================================="
          echo "    SECURITY SCAN SUMMARY"
          echo "=============================================="
          echo ""

          # Custom rules summary
          if [ -s custom.txt ]; then
            echo "❌ CUSTOM RULES (70) – FAILED:"
            cat custom.txt
            echo ""
          else
            echo "✅ CUSTOM RULES (70) – PASSED"
          fi
          echo "----------------------------------------------"

          # Positive rules summary
          if [ -s positive.txt ]; then
            echo "❌ POSITIVE RULES (7) – FAILED:"
            cat positive.txt
            echo ""
          else
            echo "✅ POSITIVE RULES (7) – PASSED"
          fi
          echo "=============================================="

          # Exit 1 if any rule failed
          if [ -s custom.txt ] || [ -s positive.txt ]; then
            echo "💥 BUILD FAILED – fix issues or add # nosemgrep justification"
            exit 1
          else
            echo "🎉 BUILD PASSED – all security rules satisfied"
          fi
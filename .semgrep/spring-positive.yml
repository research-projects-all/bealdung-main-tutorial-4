  - id: POS-escape-path-var
    pattern-either:
      - pattern: |
          @GetMapping("/...")
          public String $METHOD(@PathVariable String $X) {
            ...
            return $X;
          }
      - pattern-regex: '\$\{[^}]*\}'   # catches ${id} in JSP/HTML
    message: "🔒  **Beginner safety**: returning the user parameter directly causes **XSS**."
    severity: ERROR
    languages: [java, html]   # <-- fixed token